<div id="form">
  <h2>Edit Profile</h2>
  <form action="/edit-profile" method="POST">
    <label>Username
      <input type="text" name="proposedUser" placeholder="{{user.username}}" />
    </label>

    <label>Email
      <input type="email" name="proposedEmail" placeholder="{{user.email}}" />
    </label>

    <label>Password
      <input type="password" name="proposedPassword" placeholder="********" />
    </label>

    <button type="submit">Save Changes</button>

    {{#if errorMessage}}
    <p class="error">{{errorMessage}}</p>
    {{/if}}
  </form>
</div>